N = int(input())  # 숫자의 개수 입력
numbers = []      # 숫자들을 담을 빈 리스트 만들기

for i in range(N): # N번 반복하면서 숫자들을 입력받아 리스트에 추가
    num = int(input())
    numbers.append(num)

numbers.sort() # 리스트를 오름차순으로 정렬

for n in numbers: # 정렬된 숫자 하나씩 출력
    print(n)
